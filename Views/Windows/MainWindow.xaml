<Window x:Class="RequestManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"                
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"                           
        xmlns:vm="clr-namespace:RequestManager.ViewModels"
        xmlns:fa5="http://schemas.fontawesome.com/icons/"
        xmlns:faSh="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        xmlns:cmd="clr-namespace:RequestManager.Infrastructure.Commands"
        mc:Ignorable="d"
        Title="Менеджер запросов" Height="450" Width="950" Background="#dce6df">
    <Window.DataContext>
        <vm:RequestViewModel></vm:RequestViewModel>
    </Window.DataContext>


    <DockPanel >
        <TabControl Background="#dce6df" DockPanel.Dock="Top" Margin="1">
            <TabItem FontSize="14" >
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <faSh:IconBlock Icon="ArrowAltCircleDown" Margin="0,0,2,0" Height="14"/>
                        <TextBlock Text="Запросы" Cursor="Hand"/>
                    </StackPanel>
                </TabItem.Header>
                <Frame x:Name="MainFrame" Content="{Binding Path = CurrentPage}" NavigationUIVisibility="Visible" />
            </TabItem>
            <TabItem  FontSize="14" Background="#dce6df">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <faSh:IconImage Icon="Gears" Height="15" Margin="0,0,2,0"/>
                        <TextBlock Text="Установки" Cursor="Hand"/>
                    </StackPanel>
                </TabItem.Header>
                <DockPanel Background="#dce6df"  Height="220" VerticalAlignment="Top"  >
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="1">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
                            <Label>Менеджеры:</Label>
                            <ListBox x:Name="ManagersList" ItemsSource="{Binding Path=ConfigObject.Managers}" SelectedItem="{Binding Path=SelectedManager, Mode=TwoWay}"/>
                            <TextBox HorizontalAlignment="Left"  Foreground="SlateGray" Width="144" ToolTip="Введите имя нового менеджера" Text="{Binding NewManager, Mode=TwoWay}"></TextBox>
                            <StackPanel Orientation="Horizontal">
                                <Button Command="{Binding Path=AddItemCommand}" CommandParameter="manager">Добавить</Button>
                                <Button Command="{Binding Path=DeleteItemCommand}" CommandParameter="manager">Удалить</Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="1">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
                            <Label>Исполнители:</Label>
                            <ListBox x:Name="ExecutorsList" ItemsSource="{Binding Path=ConfigObject.Executors}" SelectedItem="{Binding Path=SelectedExecutor, Mode=TwoWay}"/>
                            <TextBox HorizontalAlignment="Left"  Foreground="SlateGray" Width="144" ToolTip="Введите имя исполнителя" Text="{Binding NewExecutor, Mode=TwoWay}"></TextBox>
                            <StackPanel Orientation="Horizontal">
                                <Button Command="{Binding Path=AddItemCommand}" CommandParameter="executor">Добавить</Button>
                                <Button Command="{Binding Path=DeleteItemCommand}" CommandParameter="executor">Удалить</Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="1">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
                            <Label>Сервер SQL:</Label>
                            <TextBox HorizontalAlignment="Left"  Foreground="SlateGray" Width="200" ToolTip="Введите имя сервера" Text="{Binding ConfigObject.Server, Mode=TwoWay}"></TextBox>
                            <Button Command="{Binding Path=SaveServerCommand}" Width="120" HorizontalAlignment="Left" Cursor="Hand">Сохранить</Button>
                        </StackPanel>
                    </Border>
                </DockPanel>
                



            </TabItem>
        </TabControl>
        
    </DockPanel>    


</Window>
